import { PhoneNumerEGDirective } from './phone-numer-eg.directive';

describe('PhoneNumerEGDirective', () => {
  it('should create an instance', () => {
    const directive = new PhoneNumerEGDirective();
    expect(directive).toBeTruthy();
  });
});
